<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#121212">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="VPN Deploy"
        android:textSize="24sp"
        android:textColor="#fff"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <EditText
        android:id="@+id/etHost"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="IP адрес"
        android:textColor="#fff"
        android:textColorHint="#888"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/etPassword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Пароль"
        android:inputType="textPassword"
        android:textColor="#fff"
        android:textColorHint="#888"
        android:layout_marginBottom="16dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <Button android:id="@+id/btnDeploy" android:layout_width="0dp" android:layout_weight="1" android:text="DEPLOY" android:layout_marginEnd="4dp"/>
        <Button android:id="@+id/btnStatus" android:layout_width="0dp" android:layout_weight="1" android:text="STATUS" android:layout_marginEnd="4dp"/>
        <Button android:id="@+id/btnConfigs" android:layout_width="0dp" android:layout_weight="1" android:text="CONFIGS"/>
    </LinearLayout>

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="16dp"
        android:background="#1a1a1a">
        <TextView
            android:id="@+id/tvOutput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#0f0"
            android:typeface="monospace"
            android:padding="8dp"
            android:text="Ready..."/>
    </ScrollView>

</LinearLayout>
